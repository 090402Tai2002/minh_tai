# tai_code 
code raspery bao gồm 
b1: code xử lý ảnh ở calibration camera cho ra pixel/mm dùng vật biết trước kích thước để hiệu chuẩn camera .
b2; dùng ml để xử lý ảnh đưa ra vùng chứa tôm và đo tôm build trên yolo v8( v11)
b3; gửi dữ liệu lên mqtt.
code esp32 :


